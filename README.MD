# First Steps

1. Create a python environment with `python -m venv .venv`
2. Activate the environment `source .venv/Scripts/activate`
3. Install requirements `pip install -r requirements.txt`
4. Start Otree server `otree devserver` 